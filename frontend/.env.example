# ============================================
# Variables de entorno — copiar a frontend/.env
# ============================================

FRONTEND_HOST=0.0.0.0
FRONTEND_PORT=5173
LAN_IP=

# URL del backend API (proxy /api)
BACKEND_URL=http://localhost:3000
