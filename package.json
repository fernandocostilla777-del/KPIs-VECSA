{
  "name": "kpis-balderrama",
  "version": "1.0.0",
  "description": "Dashboard KPIs BALDERRAMA — monorepo backend + frontend",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix desktop && npm install --prefix objetivos-web",
    "start": "concurrently -n backend,frontend -c blue,green \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "dev": "npm start",
    "start:backend": "npm run start --prefix backend",
    "start:frontend": "npm run start --prefix frontend",
    "objetivos": "npm run dev --prefix objetivos-web",
    "objetivos:build": "npm run build --prefix objetivos-web",
    "desktop": "npm --prefix ./desktop run start",
    "dist": "npm --prefix ./desktop run dist",
    "dist:win": "npm --prefix ./desktop run dist:win",
    "publish:win": "npm --prefix ./desktop run publish:win",
    "dist:mac": "npm --prefix ./desktop run dist:mac"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
